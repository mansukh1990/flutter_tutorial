/*
GetRange
  - GetRange method is used to create an iterable that iterates over a range of elements.
 */
void main() {
  var numList = [1, 3, 5, 6, 46, 65, 6565, 65656, 656, 5656];

  var getRange = numList.getRange(2, 8);

  print(getRange);
}
